apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "devops-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
